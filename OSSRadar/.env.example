# Optional: GitHub Personal Access Token.
# Without it the app uses the public API (60 requests/hour, search limited to 10/minute).
# With it you get 5,000 requests/hour — recommended for any real deployment.
# Create one at: https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=
