import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap"
});

export const metadata: Metadata = {
  title: {
    default: "NEXORA — Your company, as one intelligent digital world",
    template: "%s · NEXORA"
  },
  description:
    "The open-source platform that connects departments, communication, knowledge and AI into a single digital representation of your organization."
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
